import 'package:flutter/material.dart';

class AllServantsBodyForStage extends StatelessWidget {
  const AllServantsBodyForStage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
