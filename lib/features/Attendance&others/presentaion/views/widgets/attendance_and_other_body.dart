import 'package:flutter/material.dart';

class AttendanceAndOtherBody extends StatelessWidget {
  const AttendanceAndOtherBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('attenance & others')],
    );
  }
}
