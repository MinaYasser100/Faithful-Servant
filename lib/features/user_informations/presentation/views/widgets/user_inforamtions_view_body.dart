import 'package:flutter/material.dart';

class UserInformationsViewBody extends StatelessWidget {
  const UserInformationsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
